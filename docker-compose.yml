services:
  cloudshell_eks:
    build: .
    image: cloudshell_eks:latest
    container_name: cloudshell_eks
    volumes:
      - .:/apps
      - ~/.aws:/root/.aws
      - ~/.kube:/root/.kube
      - ~/.claude:/root/.claude
    env_file: .env